# Filename: tmux.conf
# Last change: Sat, 25 Oct 2025, 6:10PM

# Use Homebrew bash for plugin scripts (must be set EARLY)
set-environment -g PATH "/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin"

source-file ~/Documents/DEV/dotfiles/tmux/keymaps.conf
source-file ~/Documents/DEV/dotfiles/tmux/plugins.conf

# Initialize TPM (keep this line at the very bottom)
run '~/.tmux/plugins/tpm/tpm'

# Overrides (must load AFTER plugins)
source-file ~/Documents/DEV/dotfiles/tmux/overrides.conf
